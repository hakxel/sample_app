<% if logged_in? %>
  <div class="row">
    <%= render 'shared/user_home' %>
    <div class="col-md-8">
      <h3>Micropost Feed</h3>
      <%= render 'shared/feed' %>
    </div>    
  </div>
<% else %>
  <%= render 'shared/standard_home' %>
<% end %>
